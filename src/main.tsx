import React from 'react'
import ReactDOM from 'react-dom/client'
import CinemaNightApp from './CinemaNightApp'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <CinemaNightApp />
  </React.StrictMode>
)
